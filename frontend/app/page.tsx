"use client";

import { ChangeEvent, useMemo, useState } from "react";
import { Report, request, Student } from "@/lib/api";

type UploadResponse = { students: Student[]; report: Report };
type SortKey = "name" | "gender" | "grade" | "Math" | "Science" | "English" | "total" | "status";
const columns: { key: SortKey; label: string }[] = [
  { key: "name", label: "Student" }, { key: "gender", label: "Gender" }, { key: "grade", label: "Grade" }, { key: "Math", label: "Math" },
  { key: "Science", label: "Science" }, { key: "English", label: "English" }, { key: "total", label: "Total" }, { key: "status", label: "Status" },
];

export default function Home() {
  const [students, setStudents] = useState<Student[]>([]);
  const [report, setReport] = useState<Report | null>(null);
  const [threshold, setThreshold] = useState(0);
  const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(25);
  const [nameQuery, setNameQuery] = useState("");
  const [sort, setSort] = useState<{ key: SortKey; direction: "asc" | "desc" } | null>(null);
  const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  const shortlist = useMemo(() => students.filter((s) => s.status === "Active" && s.total >= threshold), [students, threshold]);
  const displayed = useMemo(() => {
    const matches = students.filter((s) => s.name.toLocaleLowerCase().includes(nameQuery.trim().toLocaleLowerCase()));
    if (!sort) return matches;
    return [...matches].sort((a, b) => {
      const left = a[sort.key]; const result = typeof left === "string" ? left.localeCompare(String(b[sort.key])) : Number(left) - Number(b[sort.key]);
      return sort.direction === "asc" ? result : -result;
    });
  }, [students, nameQuery, sort]);
  const average = shortlist.length ? (shortlist.reduce((sum, s) => sum + s.total, 0) / shortlist.length).toFixed(1) : "0";
  const pageCount = Math.max(1, Math.ceil(displayed.length / pageSize)); const currentPage = Math.min(page, pageCount); const pageStart = (currentPage - 1) * pageSize;
  const visible = displayed.slice(pageStart, pageStart + pageSize);
  function changeSort(key: SortKey) { setSort((x) => x?.key === key ? { key, direction: x.direction === "asc" ? "desc" : "asc" } : { key, direction: "asc" }); setPage(1); }
  async function upload(e: ChangeEvent<HTMLInputElement>) { const file = e.target.files?.[0]; if (!file) return; setLoading(true); setError(""); const body = new FormData(); body.append("file", file); try { const data = await request<UploadResponse>("/upload", { method: "POST", body }); setStudents(data.students); setReport(data.report); setPage(1); } catch (err) { setError(err instanceof Error ? err.message : "Upload failed."); } finally { setLoading(false); e.target.value = ""; } }
  async function toggle(s: Student) { const status = s.status === "Active" ? "Debarred" : "Active"; setStudents((xs) => xs.map((x) => x.id === s.id ? { ...x, status } : x)); try { await request<Student>(`/students/${s.id}/status`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status }) }); } catch (err) { setStudents((xs) => xs.map((x) => x.id === s.id ? s : x)); setError(err instanceof Error ? err.message : "Could not save status."); } }
  function exportCsv() { const rows = [["Name", "Gender", "Grade", "Math", "Science", "English", "Total", "Status"], ...shortlist.map((s) => [s.name, s.gender, s.grade, s.Math, s.Science, s.English, s.total, s.status])]; const csv = rows.map((r) => r.map((v) => `"${String(v).replaceAll('"', '""')}"`).join(",")).join("\n"); const a = document.createElement("a"); a.href = URL.createObjectURL(new Blob([csv], { type: "text/csv" })); a.download = "student-shortlist.csv"; a.click(); URL.revokeObjectURL(a.href); }
  return <main className="min-h-screen bg-slate-950 text-slate-100"><div className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><header className="mb-9 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold tracking-[.22em] text-cyan-400">STUDENT OPERATIONS</p><h1 className="text-3xl font-semibold sm:text-4xl">Data Pipeline <span className="text-slate-500">/</span> Shortlist</h1></div><label className="cursor-pointer rounded-lg bg-cyan-400 px-5 py-3 text-center text-sm font-bold text-slate-950 hover:bg-cyan-300">{loading ? "Cleaning data…" : "Upload raw CSV"}<input className="hidden" type="file" accept=".csv,text/csv" disabled={loading} onChange={upload} /></label></header>{error && <div className="mb-6 rounded-lg border border-rose-500/40 bg-rose-500/10 px-4 py-3 text-sm text-rose-200">{error}</div>}
    {!report ? <section className="grid min-h-80 place-items-center rounded-2xl border border-dashed border-slate-700 bg-slate-900/50 p-8 text-center"><div><p className="text-lg font-medium">Upload a student CSV to begin</p><p className="mt-2 text-sm text-slate-400">Required: Name, Gender, Grade, Math, Science, English. Total is checked and recalculated.</p></div></section> : <><section className="mb-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Metric label="Rows accepted" value={`${report.accepted_rows} / ${report.input_rows}`} /><Metric label="Rejected rows" value={String(report.rejected_rows)} tone="rose" /><Metric label="Duplicates removed" value={String(report.exact_duplicates_removed)} /><Metric label="Totals corrected" value={String(report.totals_recalculated)} /></section>
      <section className="mb-6 rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">Live shortlist</h2><p className="mt-1 text-sm text-slate-400">Only Active students meeting the threshold are included.</p></div><div className="flex items-center gap-3"><label className="text-sm text-slate-300">Minimum total</label><input aria-label="Minimum total score" className="w-24 rounded-md border border-slate-700 bg-slate-950 px-3 py-2 text-sm" type="number" min="0" max="300" value={threshold} onChange={(e) => setThreshold(Math.max(0, Number(e.target.value)))} /><button onClick={exportCsv} className="rounded-md border border-cyan-400/50 px-3 py-2 text-sm font-semibold text-cyan-300 hover:bg-cyan-400/10">Export CSV</button></div></div><div className="mt-5 grid grid-cols-2 gap-3 border-t border-slate-800 pt-4"><Metric label="Matched students" value={String(shortlist.length)} /><Metric label="Average total" value={average} /></div></section>
      <section className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900"><div className="flex flex-col justify-between gap-4 border-b border-slate-800 p-5 sm:flex-row sm:items-center"><div><h2 className="font-semibold">Cleaned roster</h2><p className="mt-1 text-sm text-slate-400">Search by name, sort a column, or toggle status.</p></div><div className="flex items-center gap-3"><input aria-label="Search students by name" className="w-56 rounded-md border border-slate-700 bg-slate-950 px-3 py-2 text-sm placeholder:text-slate-500" placeholder="Search by name…" value={nameQuery} onChange={(e) => { setNameQuery(e.target.value); setPage(1); }} /><span className="text-xs text-slate-500">{displayed.length} shown</span></div></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-950/50 text-xs uppercase tracking-wider text-slate-500"><tr>{columns.map((c) => <th className="px-5 py-3 font-medium" key={c.key} aria-sort={sort?.key === c.key ? (sort.direction === "asc" ? "ascending" : "descending") : "none"}><button onClick={() => changeSort(c.key)} className="inline-flex items-center gap-1 hover:text-cyan-300">{c.label}<span aria-hidden="true" className={sort?.key === c.key ? "text-cyan-300" : "text-slate-600"}>{sort?.key === c.key ? (sort.direction === "asc" ? "↑" : "↓") : "↕"}</span><span className="sr-only">Sort by {c.label}</span></button></th>)}</tr></thead><tbody className="divide-y divide-slate-800">{visible.map((s) => <tr key={s.id} className={s.status === "Debarred" ? "bg-rose-500/[.03] text-slate-500" : "hover:bg-slate-800/45"}><td className="px-5 py-3 font-medium text-slate-100">{s.name}</td><td className="px-5 py-3">{s.gender}</td><td className="px-5 py-3">{s.grade}</td><td className="px-5 py-3">{s.Math}</td><td className="px-5 py-3">{s.Science}</td><td className="px-5 py-3">{s.English}</td><td className="px-5 py-3 font-semibold text-cyan-300">{s.total}</td><td className="px-5 py-3"><button onClick={() => toggle(s)} className={`rounded-full px-3 py-1 text-xs font-bold ${s.status === "Active" ? "bg-emerald-400/15 text-emerald-300" : "bg-rose-400/15 text-rose-300"}`}>{s.status}</button></td></tr>)}</tbody></table></div><div className="flex flex-col gap-3 border-t border-slate-800 p-4 text-sm sm:flex-row sm:items-center sm:justify-between"><p className="text-slate-400">Showing {displayed.length ? pageStart + 1 : 0}–{Math.min(pageStart + pageSize, displayed.length)} of {displayed.length}</p><div className="flex items-center gap-3"><label className="text-slate-400">Rows <select aria-label="Rows per page" className="ml-1 rounded border border-slate-700 bg-slate-950 px-2 py-1 text-slate-200" value={pageSize} onChange={(e) => { setPageSize(Number(e.target.value)); setPage(1); }}><option value={25}>25</option><option value={50}>50</option><option value={100}>100</option></select></label><button onClick={() => setPage((p) => Math.max(1, p - 1))} disabled={currentPage === 1} className="rounded border border-slate-700 px-3 py-1 disabled:opacity-40">Previous</button><span className="text-slate-300">Page {currentPage} of {pageCount}</span><button onClick={() => setPage((p) => Math.min(pageCount, p + 1))} disabled={currentPage === pageCount} className="rounded border border-slate-700 px-3 py-1 disabled:opacity-40">Next</button></div></div></section>
      <section className="mt-6 grid gap-6 lg:grid-cols-2"><Panel title="Cleaning decisions" items={report.rules} /></section></>}
      </div>
      </main>;
}
function Metric({ label, value, tone = "cyan" }: { label: string; value: string; tone?: "cyan" | "rose" }) { return <div className="rounded-xl border border-slate-800 bg-slate-900 px-4 py-3"><p className="text-xs uppercase tracking-wider text-slate-500">{label}</p><p className={`mt-1 text-2xl font-semibold ${tone === "rose" ? "text-rose-300" : "text-cyan-300"}`}>{value}</p></div>; }
function Panel({ title, items, empty }: { title: string; items: string[]; empty?: string }) { return <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">{title}</h2>{items.length ? <ul className="mt-3 space-y-2 text-sm leading-6 text-slate-400">{items.map((item, i) => <li key={i} className="border-l-2 border-slate-700 pl-3">{item}</li>)}</ul> : <p className="mt-3 text-sm text-slate-500">{empty}</p>}</section>; }
